export const environment = {
  production: true,
  apiUrl: 'api/',//When we publish our application, we're going to publish our annual application and host it from our
  hubUrl: 'hubs/'//when we publish, we don't run into any problems
};
